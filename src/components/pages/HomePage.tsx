import { FC, memo } from 'react';
import HeaderTemplate from '../templates/HeaderTemplate';

const HomePage: FC = memo(() => <HeaderTemplate><h1>Home Page</h1></HeaderTemplate>);

export default HomePage;
